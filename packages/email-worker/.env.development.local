# Kafka
KAFKA_BROKER=localhost:9092

# Worker
WORKER_COUNT=5

# Mail Provider (gmail, smtp, outlook, sendgrid, mailgun)
MAIL_PROVIDER=gmail

# Gmail Configuration
GMAIL_USER=subhamoyghosh2017@gmail.com
GMAIL_APP_PASSWORD=hmiu oyuh xqei vvls
FROM_EMAIL=subhamoyghosh2017@gmail.com

# SMTP Configuration (used when MAIL_PROVIDER=smtp)
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=

# Outlook Configuration (used when MAIL_PROVIDER=outlook)
OUTLOOK_USER=your-email@outlook.com
OUTLOOK_PASSWORD=your-password

# SendGrid Configuration (used when MAIL_PROVIDER=sendgrid)
SENDGRID_API_KEY=your-sendgrid-api-key

# Mailgun Configuration (used when MAIL_PROVIDER=mailgun)
MAILGUN_DOMAIN=your-domain.com
MAILGUN_API_KEY=your-mailgun-api-key